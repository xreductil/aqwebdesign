# 資料目錄

- `wordpress/`：WordPress 匯出 XML 與暫存檔。
- `crawl/`：一般爬蟲產生的 JSON、CSV 結果。
- `images/`：下載的原站圖片（英文用途檔名）與 `image-map.json` 圖片來源對照表。
- `../.firecrawl/`：Firecrawl 的原始爬取、Markdown、API 與補充資料。
- `../site/`：由上述資料重新產生的本機靜態網站，不放在 `data/` 內以避免和輸入資料混淆。
