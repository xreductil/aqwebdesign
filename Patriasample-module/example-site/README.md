# Independent module example site

This Vite app imports the cart and customer modules directly. It does not import any host site's application, data, or server files.

```bash
npm install
npm run build
```

Set `VITE_API_BASE_URL` when the API is hosted separately.
